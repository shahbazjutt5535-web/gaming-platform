// Placeholder for crypto API check (TRC20, ERC20, BTC)
async function checkDeposit(address) {
  // Connect to blockchain API
  // Check if new deposit > 10 USD
  // Return deposit info
}

module.exports = { checkDeposit };
